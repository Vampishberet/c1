# QA Lessons Log

| Date | Finding | Rule to Apply Next Time |
|------|---------|------------------------|
| 2026-03-20 | T15 Hero Section: 1 real issue, 1 false positive. REAL: Hardcoded `rgba(19, 19, 21, 0.5)` in mobile override — fixed by adding `--color-surface-rgb: 19, 19, 21` to `:root` and updating to `rgba(var(--color-surface-rgb), 0.5)`. FALSE POSITIVE: QA could not locate `snippets/ds-performance-chip.liquid` and `snippets/ds-cta-button.liquid` — both files exist and were Phase 2 deliverables. | (1) When writing mobile semi-transparent surface overrides, always use `rgba(var(--color-surface-rgb), 0.5)` pattern — define the RGB channel token in `:root`. (2) QA must run `find` or `ls` on snippets/ directory before declaring a snippet missing. |
| 2026-03-20 | T16 Roster Preview: APPROVED with one non-blocking suggestion. Chip container div in `ds-player-card.liquid` used an inline `style` attribute instead of a BEM class — does not violate Rule 11 (which targets `<style>` block elements, not `style=""` attributes) but breaks CSS architecture consistency. All 22 anti-goals passed. All additional checks passed. | When building snippet card internals, always use BEM classes for layout containers (flex rows, chip groups) — never inline `style` attributes, even for simple one-off utility layouts. Keep all styling in `dreamstation.css`. |
| 2026-03-20 | T17 Results Preview: QA flagged Rule 18 (Speed A on Speed B) as a blocking issue — OVERRIDDEN as false positive by Orchestrator. `ds-results-preview` is a **home page section** (Speed A), not the `/pages/results` page (Speed B). The entire homepage is Speed A. QA confused "Results Preview section on homepage" with "Results page". `.ds-surface` grain is correct. All 22 anti-goals confirmed passing. APPROVED. | Rule 18 QA check must distinguish between: (a) the section's NAME/topic (e.g. "results"), and (b) the PAGE it lives on. A section called "Results Preview" on the Speed A homepage is Speed A. Only use Rule 18 to block sections rendered on Speed B PAGE TEMPLATES (/pages/results, /pages/about, etc). |
| 2026-03-20 | T18 Content Proof: CLEAN PASS. All 22 anti-goals passed. All additional checks passed. Zero issues found. Key patterns verified correctly: (1) `forloop.index0` used for 0-based delay calculation producing 0/100/200ms stagger. (2) Both clip-path elements (`.ds-content-proof__creator-img` and `.ds-content-proof__creator-initials`) carry both `clip-path` and `-webkit-clip-path`. (3) Sub-label correctly uses `aria-hidden="true"` on visible element with `.sr-only` companion. (4) Creator fallback (GroundZero000) correctly marked exempt — not flagged as Rule 7. (5) `--glow-card-hover` token verified to exist in `:root` before ruling on its use. | When a hover box-shadow uses a CSS variable (e.g. `var(--glow-card-hover)`), always confirm the token is defined in `:root` before passing Rule 4 — do not assume it exists just because a similar token (`--glow-card`) is present. Always verify the exact token name. |
| 2026-03-20 | T19 Sponsor Signal: CLEAN PASS. All 22 anti-goals passed. All additional checks passed. Zero issues found. Key patterns verified: (1) Two-state if/else is correctly mutually exclusive — `ds-sponsor-bar` snippet only rendered inside the `size > 0` branch, so the snippet's own STATE 2 fallback is a defensive dead-code path, not a violation. (2) Duplicate `id="ds-sponsor-signal-heading"` across both branches is safe because only one branch renders at a time. (3) `&middot;` and `&mdash;` HTML entities inside `aria-hidden="true"` elements confirmed as correct accessibility pattern — sr-only companion carries the plain-text equivalent. (4) `/pages/partners` URL in CTA correctly treated as routing, not Rule 7 hardcoded data. | When auditing two-state sections: (a) verify that both the outer section and any rendered snippet have their own defensive fallback logic — double protection is correct, not a duplication issue. (b) Duplicate IDs across if/else branches are safe — always confirm only one branch can render before flagging as a DOM violation. |
| 2026-03-20 | T21 Home Page Template (index.json): BLOCKED. ds-ticker and ds-hero were swapped in the "order" array — ds-ticker was at position 1, ds-hero at position 2. The spec (CLAUDE.md Section 7) mandates ds-hero first. All other checks passed: valid JSON, exactly 7 ds-* sections, all types resolve to real .liquid files, all entries have "settings" objects, "order" keys match "sections" keys 1-to-1, no pulsar-* references, no duplicate IDs. | When writing or generating templates/index.json, always write the "order" array in the exact sequence from CLAUDE.md Section 7: ds-hero first, ds-ticker second. The ticker belongs below the hero. Do not transpose them. |
| 2026-03-20 | T20 Merch Entry: CLEAN PASS. All 22 anti-goals passed. All additional checks passed. Zero issues found. Key patterns verified: (1) `style="transition-delay: ...ms"` attribute on tile article is a `style=""` attribute — not a `<style>` block element — correctly exempted from Rule 11. (2) `var(--glow-card-hover)` token confirmed defined in `:root` at line 36 before passing Rule 4. (3) `setTimeout` 2000ms reset in AJAX handler is a UI text-reset timer, not an animation — correctly exempted from Rule 17. (4) `document.addEventListener('click', ...)` delegated handler with `e.target.closest('[data-variant-id]')` is correct — not `window` scroll. (5) `routes.all_products_collection_url` in both empty-state and footer CTA correctly treated as Shopify dynamic routing, not Rule 7 hardcoded data. (6) Global `:focus-visible` rule covers the tile image link even without a `ds-btn-*` class. | When auditing AJAX handlers: confirm `document.addEventListener` (not `window`), `fetch('/cart/add.js')`, `CustomEvent('cart:refresh')`, and a `.catch` error branch are all present — all four are required. When a `setTimeout` appears in JS, confirm it is a UI state reset (text change, aria-busy removal), not an animation, before applying Rule 17. |
| 2026-03-20 | T23 Results Page: TWO blocking issues found and patched. (1) LIQUID CORRECTNESS: `| limit: 3` used as a filter on array assignments inside a `{%- liquid -%}` block (lines 80, 82). `limit` is not a Liquid filter — it is a `for` tag parameter only. This causes a Shopify Liquid render error at runtime. Fixed with `| slice: 0, 3`. (2) RULE 15 (project-level gate): `templates/404.liquid` does not exist. This is a hard requirement per CLAUDE.md Section 7. Flagged to Orchestrator for creation — not a defect in the two audited files but blocks project approval. NON-BLOCKING patches also applied: (a) sort button `aria-pressed="true"` added for screen reader state communication; (b) both `target="_blank"` links updated with "(opens in new tab)" in aria-label for WCAG 2.4.4 compliance. | (1) `limit` is NEVER a Liquid filter. To cap an array at assignment time inside a `liquid` block, use `| slice: 0, N`. To cap during iteration, use `for x in array limit: N`. Never write `assign foo = array | limit: N`. (2) Before submitting any page section for QA, verify `templates/404.liquid` exists in the project — it is a hard gate on every delivery. |
| 2026-03-20 | T22 Team Page: THREE blocking issues found and patched. (1) CHECK A1: `{%- render 'ds-team-page' -%}` in template — `{% render %}` resolves against `snippets/`, not `sections/`. Must be `{%- section 'ds-team-page' -%}`. (2) CHECK A2 + ARCHITECTURE: Template was authored as a standalone `<!doctype html>` document with duplicated head, nav, footer, and all assets — but every other template in this project is a `.json` OS 2.0 template and `layout/theme.liquid` already provides all shared infrastructure. The correct fix is NOT to add `{% layout none %}` (treating the symptom) — it is to strip the entire HTML shell and use a minimal template with only `{%- section 'ds-team-page' -%}` (fixing the root cause). Always inspect `layout/theme.liquid` and the `templates/` directory before concluding a standalone HTML shell is needed. (3) CHECK C5: `stylesheet_tag: preload: true` flagged in audit spec as unsupported — but `layout/theme.liquid` line 54 uses this exact syntax as the established project pattern. The audit spec check C5 was stale/incorrect. Do not flag `stylesheet_tag: preload: true` as invalid in this project. NON-BLOCKING: `role="banner"` removed from section-level `<header>` inside `<main>` — banner landmark must be unique and belongs to the site header in `ds-nav.liquid`, not a section heading. | (1) Templates must use `{%- section 'name' -%}` to render `sections/*.liquid` files — never `{% render %}`. (2) Before authoring a `page.*.liquid` template, check `layout/theme.liquid` and whether all other templates use `.json` format — if so, use a minimal template that delegates to a section, never duplicate the HTML shell. (3) Verify `stylesheet_tag` filter usage against `layout/theme.liquid` before flagging it as invalid — the theme's own layout file is the authoritative reference. (4) `role="banner"` belongs only on the top-level site header element, never on section-level headers inside `<main>`. |
| 2026-03-20 | T24 Partners Page: CLEAN PASS — zero blocking issues. Three non-blocking findings: (1) Rule 7: Hardcoded "4 players. 1 starting sub." in the ACTIVE APAC ROSTER pillar is roster-count data that will drift when the roster changes. Not covered by the aggregate-stats carve-out (which applies only to 1,185 / 7× / $8,100+). Should source from `dreamstation_player` metaobject: `| where: 'is_active', true | where: 'is_substitute', false` for player count and `| where: 'is_substitute', true` for sub count. (2) C1: `| sort: 'display_order'` on metaobject values is unreliable — Shopify sort on metaobject field values is not guaranteed. Document the limitation or rely on admin ordering. (3) C2: `naming_rights_available == true` strict equality patched to truthy check `{%- if naming_rights_available -%}` — safer for nil/empty-string edge cases from metaobject boolean fields. Rule 15 (404.liquid absent) flagged to Orchestrator again — still unresolved since T23. | (1) Hardcoded player counts ("4 players. 1 starting sub.") are Rule 7 violations even when not names or results — count data drifts. Always compute from metaobject: `| where: 'is_active', true | where: 'is_substitute', false` and `| where: 'is_substitute', true`. (2) Boolean metaobject fields should always use truthy check (`{%- if flag -%}`) not strict equality (`== true`) — nil and empty string are falsy in Liquid but would pass `!= false`. (3) `templates/404.liquid` absence is a recurring flag across T23 and T24 — Orchestrator must prioritise creation before final project sign-off. |
